# Zig build artifacts
.zig-cache/
zig-out/
zig-pkg/

# Pinned reference checkouts (restored by tools/fetch-reference.sh; never
# committed). The trailing-slash form only matches real directories, so the
# symlink form is listed explicitly too.
.reference/
/.reference

# Editor/tooling droppings
*.o
*.a
.DS_Store
